import { chromium } from "playwright";
const b=await chromium.launch({headless:true}); const p=await b.newPage({viewport:{width:1000,height:700}}); await p.goto('http://localhost:3000/pdf-tools/edit-pdf',{waitUntil:'networkidle'}); const data=await p.evaluate(()=>[...document.querySelectorAll('*')].map(n=>{const r=n.getBoundingClientRect(),s=getComputedStyle(n);return {tag:n.tagName,class:n.className,position:s.position,bg:s.backgroundColor,shadow:s.boxShadow,border:s.border,rect:{left:r.left,right:r.right,top:r.top,bottom:r.bottom,width:r.width,height:r.height}}}).filter(x=>x.position==='fixed'||x.bg==='rgb(0, 0, 0)')); console.log(JSON.stringify(data,null,2)); await p.screenshot({path:'tmp-apply-strip.png'}); await b.close();
