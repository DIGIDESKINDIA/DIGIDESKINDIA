import fs from "fs";
async function run(name, path) { const f=new FormData(); f.append("file",new Blob([fs.readFileSync(path)],{type:"application/pdf"}),`${name}.pdf`); const started=performance.now(); const r=await fetch("http://localhost:3001/api/pdf/pdf-to-pptx",{method:"POST",body:f,signal:AbortSignal.timeout(180000)}); const b=Buffer.from(await r.arrayBuffer()); console.log(name,JSON.stringify({status:r.status,ms:Math.round(performance.now()-started),bytes:b.length,pages:r.headers.get("x-pdf-page-count"),text:r.headers.get("x-pptx-editable-text-objects"),shapes:r.headers.get("x-pptx-editable-shape-objects"),tables:r.headers.get("x-pptx-native-table-objects"),fallback:r.headers.get("x-pptx-raster-fallback-pages"),signature:b.subarray(0,2).toString("ascii")})); }
for(const [name,path] of [["table","test-files/table-fixture.pdf"],["one-page","test-files/editable-fixture.pdf"],["five-page","test-files/multi-page.pdf"],["ten-page","test-files/large.pdf"],["hindi","test-files/hindi-fixture.pdf"],["ocr","test-files/ocr-fixture.pdf"]]) await run(name,path);
